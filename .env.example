# Copy this file to .env and fill values locally.
# Never commit your real .env file.

# Anthropic API key used by AI World
ANTHROPIC_API_KEY=sk-ant-REPLACE_ME

# Optional local dev port for serve.js (default is 3000)
PORT=3000
